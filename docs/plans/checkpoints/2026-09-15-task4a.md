# Task 4a Checkpoint: 2026-09-15

## Verification Checks

### Check 1: Raman references in thf-plus-x3delta1-effective-hamiltonian.md

Command:
```
rg -n -i 'raman' docs/thf-plus-x3delta1-effective-hamiltonian.md
```

Output:
```
844:**What was not available for §9.** The two questions that would close OPEN-17 need the authors of PRA 98, 042502 (2018); no erratum or later restatement of their Eqs. (19)/(22)/(23) was found. Long, *The Raman Effect* (2002) — the conventional home of the Placzek polarisability derivation named in [2γ] §3.4 — was again not accessible, so §9.5's closure form rests on B&C (5.141)/(5.142) and Cossel's thesis, not on that textbook. Bonin & McIlrath, JOSA B **1**, 52 (1984) remains abstract-level only. Kälber et al., Z. Phys. A **334**, 103 (1989), the one paper that might carry a ²²⁷Th hyperfine measurement, is still paywalled.
1362:**Which polarisation goes in which tensor slot.** The **default ladder reading** has the operator `T_eff = Σ_i (d·ε₂)|i⟩⟨i|(d·ε₁)/Δ_i`: the **bra-side** factor carries `ε₂` (unconjugated, both photons absorbed in the same rotational direction) and the **ket-side** factor carries `ε₁`. The **Raman reading** (available as `reading='raman'`) conjugates the bra-side polarization: `T_eff = Σ_i (d·ε₂*)|i⟩⟨i|(d·ε₁)/Δ_i`. B&C (5.142) writes the Raman form — `⟨j‖T^{k₁}(A₁)‖j″⟩⟨j″‖T^{k₂}(B₁)‖j′⟩`, bra-side leg first — so slot 1 is the bra-side leg (ε₂ for ladder, ε₂* for Raman) and slot 2 is the ket-side leg (ε₁ in both). Write
1366:Raman (reading='raman'): ε_a ≡ ε₂*   (slot 1, bra-side leg)   ε_b ≡ ε₁   (slot 2, ket-side leg)
1369:**Neither the conjugation nor the ordering is cosmetic**: the ladder and Raman readings give different functions of the two Jones vectors (checked below: the two differ by up to `1.3 × 10¹` on random ε's, and they swap which physical polarisation pair reaches `Δm_F = ±2` — see row (d) for ladder, row (e) for Raman in §9.5.1(3)).
1418:  (e) same, RAMAN reading, available as `reading='raman'`  T_eff = (d.eps2*)(d.eps1)/Delta
1427:Rows (a) and (b) are identities to machine precision; row (c) shows the check is **not** vacuous — the pre-fix line fails it by `13`. Rows (d)/(e) reproduce the [2γ] §3.3 probe (`p₁ + p₂ = +1+1 → Δm_F = +2`, `+1−1 → 0`, `−1−1 → −2`) and pin down what "σ⁺σ⁺" means: row (d) is the **default ladder reading** (both photons absorbed, no conjugate). The probe's rows are labelled by the **leg components** `(p_a, p_b)` only in row (e), the **Raman reading** (available as `reading='raman'`), where conjugating `ε₂` flips the sign of its helicity label. `[derived]` The reachable **set** `Δm_F ∈ {0, ±2}` under σ± only (§9.5.4) is the same either way; only the mapping from beam polarisations to `Δm_F` differs, and it is inverted between the ladder reading (rows d, the default) and the Raman reading (rows e, available as `reading='raman'`). **Any polarization-labelled result must state which reading it uses.**
1459:**The order at which it reappears** `[derived]`. Writing `1/Δ_i = (1/Δ)(1 − δ_i/Δ + …)` with `δ_i = E_i − Ē`, the zeroth-order term is the closure term above and vanishes; the first-order term is `−(1/Δ²) P_X d_a (H − Ē) d_b P_X`, whose antisymmetric part `−(1/2Δ²) P_X (d_a H d_b − d_b H d_a) P_X` does **not** vanish. So `K = 1` is suppressed by one power of **(intermediate splitting)/(detuning)** — equivalently, it is the well-known antisymmetric Raman tensor, which exists only when the two time-orderings' denominators differ (resolved intermediates, or `ω₁ ≠ ω₂`).
1495:`p_a, p_b` are the **leg** components of §9.5.1(3), not the beams' helicity labels: which pair of beam polarisations realises a given `(p_a, p_b)` depends on whether the second photon is conjugated (Raman) or not (ladder), and the two readings are inverted with respect to each other — see the table in §9.5.1(3). The reachable **set** `{0, ±2}` is the same in both.
1497:The narrowing to `{0, ±2}` is the operational statement: Ng's target `|J=1, F=3/2, m_F=+3/2⟩ → |m_F=+1/2⟩` is `Δm_F = −1` and genuinely out of reach with σ± only, exactly as he says; `m_F = +3/2 → −1/2` is `Δm_F = −2` and **is** reachable with a same-helicity σ⁻σ⁻ pair in the default ladder reading, or (with `reading='raman'`) with `ε₁ = σ⁻, ε₂ = σ⁺` in the Raman reading of §9.5.1(3) ([2γ] §3.3, `[derived]` there, not claimed to be JILA's intent).
```

Status: PASS ✓ All lines reference `reading='raman'` option, row (e) Raman reading, or literature mentions.

### Check 2: test_K0_is_identity references in docs/

Command:
```
rg -n 'test_K0_is_identity' docs/thf-plus-x3delta1-effective-hamiltonian.md
```

Output:
```
1447:This is the algebraic content of [2γ] §3.3's table (Ω = +1 → −1 requires an Ω = 0 intermediate, because each E1 leg has `|q_i| ≤ 1` and `ΔΩ = q₁ + q₂`). The K = 0 channel matrix is exactly the identity on any basis (probe and gate `test_K0_is_identity`, 2026-09-15); transitions are carried by K = 2 alone.
```

Status: PASS ✓ Exactly one hit (top-level docs files only).

## Files Modified

1. ✓ docs/thf-plus-x3delta1-effective-hamiltonian.md
   - Swapped rows (d) and (e) in S9.5.1(3) polarization table
   - Updated prose describing row assignments
   - Rewrote "Which polarisation goes in which tensor slot" paragraph
   - Added K = 0 identity sentence to S9.5.2
   - Updated Ng reference at line 1497
   - Added audit reference to S9.5 header

2. ✓ docs/open-questions.md
   - OPEN-21: Added transition-matrix pipeline note
   - OPEN-23: Added plot README caveat

3. ✓ docs/architecture.md
   - Added transition.py operator-agnostic sandwich paragraph

## Status

All checks PASS. Ready for commit.
